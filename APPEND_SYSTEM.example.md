# Personal Instructions

Copy this file to `APPEND_SYSTEM.md` and customize it with your personal identity, preferences, and workflow notes.

Examples:

- I am `<github-username>` aka `<name>` on GitHub and other places.
- Prefer concise answers.
- When creating GitHub references, use my GitHub username `<github-username>`.
- Prefer `<tool-or-workflow>` when multiple good options exist.
